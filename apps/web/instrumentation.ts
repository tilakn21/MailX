/* eslint-disable no-process-env */

// Removed Sentry integration to make application lighter
export function register() {
  // Sentry initialization removed
}

// Placeholder function that does nothing to maintain API compatibility
export const onRequestError = () => {};
