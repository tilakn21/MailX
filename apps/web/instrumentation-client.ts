// Client-side Sentry initialization removed to make application lighter

import { env } from "@/env";

// Placeholder function that does nothing to maintain API compatibility
export const onRouterTransitionStart = () => {};
