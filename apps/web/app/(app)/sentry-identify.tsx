"use client";

// Removed Sentry integration to make application lighter
export function SentryIdentify({ email }: { email: string }) {
  // No functionality - just a placeholder component
  return null;
}
